<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="cr_patient_tree_view" model="ir.ui.view">
        <field name="name">res.partner.tree</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <tree string="patient">
                <field name="name"/>
                <field name="sex"/>
                <field name="age"/>
                <field name="blood_group"/>
                <field name="date_of_birth"/>
                <field name="marital_status"/>
                <field name="state"/>
                <!--                <field name="user_id"/>-->
            </tree>
        </field>
    </record>

    <record id="cr_patient_form_view" model="ir.ui.view">
        <field name="name">res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <form string="Patient">
                <header>
                    <button name="%(action_wizard_create_opd)d" string="Book Appointment" type="action"
                            class="oe_highlight"
                            attrs="{'invisible':[('state','not in',['approve'])]}"/>
                    <button attrs="{'invisible':[('state','in',['pending','approve','reject'])]}"
                            name="pending" string="Pending" type="object" class="oe_highlight"/>
                    <button attrs="{'invisible':[('state','in',['draft','approve','reject'])]}"
                            name="create_user" string="Confirm" type="object" class="oe_highlight"/>
                    <button attrs="{'invisible':[('state','in',['reject'])]}"
                            name="cancel" string="Reject" type="object" class="oe_highlight"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,pending,approve,reject"/>
                </header>
                <sheet>
                    <div class="oe_button_box">
                        <field name="opd_count" invisible="1"/>
                        <button name="open_opd_view" type="object" class="oe_stat_button"
                                attrs="{'invisible':[('opd_count','=', 0)]}" icon="fa-bars">
                            <field name="opd_count" widget="statinfo" string="OPD"/>
                        </button>
                        <field name="pharmacy_count" invisible="1"/>
                        <button name="open_pharmacy_view" type="object" class="oe_stat_button"
                                attrs="{'invisible':[('pharmacy_count','=', 0)]}" icon="fa-bars">
                            <field name="pharmacy_count" widget="statinfo" string="Pharmacy"/>
                        </button>
                        <field name="laboratory_count" invisible="1"/>
                        <button name="open_laboratoray_view" type="object" class="oe_stat_button"
                                attrs="{'invisible':[('laboratory_count','=', 0)]}" icon="fa-bars">
                            <field name="laboratory_count" widget="statinfo" string="Laboratory"/>
                        </button>
                        <field name="radiology_count" invisible="1"/>
                        <button name="open_radiology_view" type="object" class="oe_stat_button"
                                attrs="{'invisible':[('radiology_count','=', 0)]}" icon="fa-bars">
                            <field name="radiology_count" widget="statinfo" string="Radiology"/>
                        </button>
                    </div>
                    <field name="image_1920" widget="image" class="oe_avatar"/>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only" string="Patient Name"/>
                        <h1>
                            <field name="name" default_focus="1" placeholder="Name" required="True"/>
                        </h1>
                    </div>

                    <group>
                        <group>
                            <field name="date_of_birth" required="True"/>
                            <field name="age" readonly="True" force_save="1"/>
                            <field name="sex"/>
                            <field name="marital_status"/>
                            <field name="user_id" readonly="1"/>
                            <field name="enter_date" readonly="1"/>
                        </group>
                        <group>
                            <field name="blood_group" required="True"/>
                            <field name="is_patient" invisible="1"/>
                            <field name="doctor_department_id" options="{'no_create':True}"
                                   attrs="{'invisible':[('state','not in', ['approve'])]}"/>
                            <field name="doctor_id" domain="[('state', '=', 'approve'), ('is_doctor', '=', True),
                             ('doctor_department_id', '=', doctor_department_id)]"
                                   options="{'no_create':True}"
                                   attrs="{'invisible':[('state','not in', ['approve'])]}"
                                   context="{'form_view_ref':'cr_medical_base.cr_doctor_form_view'}"/>

                        </group>
                    </group>

                    <group string="Personal Info" name="personal_info">
                        <group>
                            <label for="street" string="Address"/>
                            <div class="o_address_format" name="div_address">
                                <field name="street" placeholder="Street..." class="o_address_street"/>
                                <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                                <field name="city" placeholder="City" class="o_address_city"/>
                                <field name="state_id" placeholder="State"/>
                                <field name="country_id" placeholder="Country"/>
                            </div>
                        </group>
                        <group>
                            <field name="email" required="True"/>
                            <field name="mobile" required="True"/>
                            <field name="confirmation_ref"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>


    <record id="patient_search_view" model="ir.ui.view">
        <field name="name">res.partner.search</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <search string="Search Sales Order">
                <field name="name"/>
                <field name="age"/>
                <filter string="Age" name="age" domain="[('age','>', 18)]" help="To Search Adult"/>
                <filter string="Name" name="name" domain="[('name', '=', 'bbb')]" help="To search same name"/>
                <separator/>
                <group expand="0" string="Group By">
                    <filter string="Age" name="age" domain="[]" context="{'group_by':'age'}"/>
                    <filter name="enter_date" string="Today's Patient"
                            domain="[('enter_date', '=', datetime.datetime.now())]"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_cr_patient_information" model="ir.actions.act_window">
        <field name="name">Patient Information</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('is_patient','=',True)]</field>
        <field name="context">{'default_is_patient':True,'search_default_enter_date':1}</field>
        <field name="view_ids"
               eval="[(5, 0, 0),
                     (0, 0, {'view_mode': 'tree', 'view_id': ref('cr_patient_tree_view')}),
                     (0, 0, {'view_mode': 'form', 'view_id': ref('cr_patient_form_view')})]"/>
    </record>

    <menuitem id="cr_medical_menu_root" name="CR Medical" sequence="1" active="True"/>
    <menuitem id="cr_menu_patient" name="Patient" parent="cr_medical_base.cr_medical_menu_root" active="True"
              sequence="2" groups="cr_medical_base.group_patient,cr_medical_base.group_admin"/>
    <menuitem id="cr_menu_configuration" name="Configuration" parent="cr_medical_base.cr_medical_menu_root"
              active="True" sequence="100"/>
    <menuitem id="cr_menu_patient_information"
              name="Patient Information"
              action="cr_medical_base.action_cr_patient_information"
              parent="cr_medical_base.cr_menu_patient"
              active="True"
              sequence="1"/>
</odoo>