<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="room_types_view_tree" model="ir.ui.view">
        <field name="name">room.types.tree</field>
        <field name="model">room.types</field>
        <field name="arch" type="xml">
            <tree string="room_types">
                <field name="name"/>
                <field name="room_facility"/>
            </tree>
        </field>
    </record>

    <record id="room_types_view_form" model="ir.ui.view">
        <field name="name">room_types</field>
        <field name="model">room.types</field>
        <field name="arch" type="xml">
            <form string="room types">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="room_facility"/>
                    </group>
                    <notebook>
                        <page string="room number" name="room_number">
                            <field name="room_ids">
                                <tree editable="bottom">
                                    <field name="room_type_ids" widget="many2many_tags"/>
                                    <field name="room_number"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="room_types_view_kanban" model="ir.ui.view">
        <field name="name">room.types.kanban</field>
        <field name="model">room.types</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="name"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click o_kanban_record_has_image_fill o_res_partner_kanban">
                            <div>
                                <div class="oe_kanban_details d-flex flex-column justify-content-between">
                                    <strong class="o_kanban_record_title oe_partner_heading">
                                        <field name="name"/>
                                    </strong>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="room_types_action" model="ir.actions.act_window">
        <field name="name">Room Types</field>
        <field name="res_model">room.types</field>
        <field name="view_mode">tree,form,kanban</field>
    </record>

</odoo>